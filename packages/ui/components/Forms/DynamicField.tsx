import { ValueType } from "@type/common";

export type DynamicFieldProps = {
  type: ValueType;
};

const DynamicField: React.FC<DynamicFieldProps> = ({ type }) => {
  return <></>;
};

export default DynamicField;
